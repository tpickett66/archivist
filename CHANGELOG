v1.0.4
* added options for associating archived records to a non-archived record
* associated archived records with original (if allowing multiple copies)
* added method_missing to ::Archive class to allow calling original class' methods

V1.0.3
* Added serialized attribute support
* merged in all changes from schubert's fork

V1.0.2
* Changed archiving functionality to persist original ID

V 1.0.0
Initial release supports the basic functionality I expected to need (copy/restore to/from archive)
