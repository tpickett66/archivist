development:
  adapter: <%= (ENV['ADAPTER_TYPE'] == 'jdbc' ? 'jdbcmysql' : 'mysql2')%>
  database: archivist
  username: root
  password: 
  host: localhost

test:
  adapter: <%= (ENV['ADAPTER_TYPE'] == 'jdbc' ? 'jdbcmysql' : 'mysql2')%>
  database: archivist
  username: root
  password: 
  host: localhost
